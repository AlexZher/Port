using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class LinesColliders : MonoBehaviour
{
    GameObject[] list;
    // Start is called before the first frame update
    void Start()
    {
        // автоматически генерировать коллайдеры линий
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
